change名稱 add-persistent-grammar-level-route-switcher

針對目前畫面上路由切換按鈕要進行調整：
1. 
目前路由按鈕 paaing 為 0.3125rem 0.5rem，要改成 0.3rem 0.39rem

2. 
由於手機模式下，未來在擴充上一定會有問題，所以路由切換的按鈕要進行調整。

調整主路由切換列中的文法路由按鈕，建立可持久保存、可擴充的 JLPT 文法等級路由切換器。
目前主路由切換列有「字母練習 / 變化規則 / N5文法 / 單字練習」。希望把「N5文法」改成可選擇 JLPT 文法等級的路由切換按鈕。

需求如下：
1. 主路由列上的文法按鈕文字要顯示目前選定的文法等級，例如「N5文法」或「N1文法」。
2. 預設選定等級為 N5。
3. 目前只有 N5 文法路由，請新增 N1～N4 文法路由。
4. 文法等級路由為 /n1-grammar、/n2-grammar、/n3-grammar、/n4-grammar、/n5-grammar。
5. N1～N4 文法路由目前內容可以是空白或簡單 placeholder，只要路由可以正常進入且不產生錯誤即可。
6. 在非文法等級頁面，例如「單字練習」頁，點擊主路由列上的文法按鈕時，要直接跳到目前選定等級的文法路由。
7. 在目前選定等級的文法路由頁面時，點擊主路由列上的文法按鈕，不要重新跳轉，而是打開一個子列表。
8. 子列表內提供 N1文法、N2文法、N3文法、N4文法、N5文法按鈕。
9. 點擊子列表中的任一文法等級後，要跳轉到該等級的文法路由，並更新主路由列上的文法按鈕文字。
10. 使用者切換後的文法等級要保存到本地儲存。關掉網頁後，下次重新開啟網站時，主路由列上的文法按鈕文字與跳轉目標都要維持上次關閉前的選擇。
11. 子列表的開啟狀態不需要保存。重新開啟網頁時，子列表一律為關閉狀態。
12. 如果本地儲存中的文法等級記憶值與目前程式碼設定不符合，例如記憶值不是有效等級、對應路由不存在、或該等級已從設定中移除，系統要刪除該記憶值並回到預設 N5，避免不必要的錯誤。
13. 子列表功能需要組件化，不能把展開列表、選項渲染、開關狀態與外部點擊關閉邏輯全部寫死在 RouteTabs 裡。
14. 請抽出可重用的路由切換子列表元件，讓未來可以用同一套機制擴充其他類似的路由下拉列表。
15. 子列表元件應以 options/config 驅動，例如 label、route、value、testId 等資料由外部傳入。
16. 子列表元件負責呈現、開關、點擊外部關閉與選項點擊事件；具體要跳去哪個路由、要保存哪個偏好值，應由父層或 composable 處理。
17. 文法等級 N1～N5 的資料不要散落在 template 裡，應集中成設定資料，方便未來新增、刪除或調整順序。
18. 子列表內的 button 樣式要與目前主路由切換列的 button 樣式一致。
19. 子列表位置在該文法按鈕下方 4px，並懸浮於畫面，不推動原本 layout。
20. 子列表按鈕之間 gap 為 2px。
21. 子列表 padding 為 0。
22. 子列表背景要與目前專案背景顏色接近，但更深一點。
23. 子列表出現時，使用者只能點擊子列表 button 與主路由切換 button。點擊其他畫面上的物件時，不執行原本動作，只關閉子列表。



