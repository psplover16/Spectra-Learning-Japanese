子路由-單字練習，實際使用之後，有些部分需要修改

目前下方table (以下稱為 單字table)，裡面是放單字，然後有一個滾動條。

先執行 1步驟。
1. 把 操作區塊的 漢字的checkbox 目前位置改到 只顯示註記的checkbox的左側。 全部字音checkbox不用理會。

2. 
切換路由 UI及排版不動，會動的只有該頁面。
預設的樣式是現在的樣式(執行 1步驟之後的樣式)。

我希望改成在不影響我目前 視覺 得情況下，結構都可以改變，做出以下效果: 
滾動條往下滾的時候，單字table會覆蓋 操作區塊(切換路由 及 單字table)，基本上滾動調往下滾1px，那單字table也會往上覆蓋1px。
覆蓋到操作區塊的畫面 直到只剩下 搜尋input及全部字音checkbox那一行時，就不在覆蓋，然後 全部字音checkbox隱藏，變成一個按鈕，按鈕是 menu, menu按鈕背景色是 淺綠色。
觸發那個淺綠色 menu按鈕，會讓 操作區塊 彈出來(此為特效，看起來像是操作區塊 由上至下彈出，特效過程其實都是浮在單字table之上)，操作區塊完全彈出之後，menu按鈕顏色變成紅色。
紅色menu按鈕的功能與綠色menu相反，會讓 操作區塊 由下往上收納(此為特效，看起來像是操作區快快速變矮，特效過程一開始是在浮在單字table之上，但到最後則會變回  在 單字table之下，遍回原本的樣子)


但是當

要注意測驗的層級(z-index)要比 本次修正層級高。

第3步驟則是本次要做的功能及考核。
3. 目前 單字table最下方的單字無法看到，被遮蓋了，此bug要作為本次的檢查點


4. 目前單字表有出現 單字 完全相同，但散落在不同 stage 的情形 (包含 只看 text 欄位完全相同、但 stage 不同)
本次要把它們做合併，規則與之前spec相同。 請確認是否有寫規格，未來新增單字，也要避免現在這種情況
