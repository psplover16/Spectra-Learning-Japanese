change名稱 add-n5-grammar-section-completion

子路由 N5文法，目前頁面內有許多container，分別收錄不同的日文用法。
每個 container 有 用 ▼ 與 ▲ 以及 容器標題的div背景色的切換，來表達容器開合狀態 

我希望改成
1. 表達開合狀態的方式，只留下 容器標題的 背景色，但背景色 在打開後要更深。
2. 原本放置  ▼ 的UI，要改成 checkbox，點該 checkbox不會觸發container 開關收合狀態，checkbox被勾選的狀態時，container無法打開，已經打開的會收合。
該狀態在使用者關閉網頁後，下次開啟 checkbox的狀態要還在。
此功能主要是用來作為 學習已完成的 註記。


